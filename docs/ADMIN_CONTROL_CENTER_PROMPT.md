haz un documento con todo el proceso que has hecho para crear el dashboard de Admin Control Center me parece una genialidad quiero que este absolutamente todo el proceso pero no solo centrado en terraleads y sus planes y cosas sino que sirva para cualquier repo de typescript y react con backend en supabase. crea un documento perfecto y dame la ruta para que se vea igual en otros proyectos

Documento creado. La ruta es:

docs/ADMIN_CONTROL_CENTER_BLUEPRINT.md

Es un documento de ~900 líneas que cubre absolutamente todo el proceso, paso a paso, y totalmente genérico para cualquier proyecto React + TypeScript + Supabase:

Lo que incluye:
Visión general con diagrama de arquitectura en capas
Fase 1 — Base de datos: tablas admin_users, admin_audit_logs, feature_flags, system_config, con RLS USING (false) y el patrón de seguridad completo
Fase 2 — RPCs: el patrón base de una RPC admin, catálogo completo de las 20+ RPCs (lectura y mutación), con ejemplos de paginación, búsqueda, ordenamiento, y protección de roles jerárquicos
Fase 3 — TypeScript: todas las interfaces que actúan como contrato entre SQL y frontend
Fase 4 — Servicios: patrón de servicios como objetos con métodos async, fallbacks resilientes, facade, barrel exports
Fase 5 — Componentes compartidos: AdminMetricCard, AdminChart (Recharts), AdminStatusBadge, AdminConfirmDialog (con confirmación por texto), AdminExportButton
Fase 6 — Layout: sidebar con secciones agrupadas, mobile dropdown, breadcrumbs, badge de rol
Fase 7 — Guard: triple verificación (auth loading → admin check → render/redirect)
Fase 8 — Páginas: patrón común con loading skeleton, error state, y contenido
Fase 9 — Rutas: cadena de protección ProtectedRoute → AdminGuard, lazy loading
Fase 10 — SEO/Seguridad: robots.txt, meta noindex, headers
Resumen de los 35+ archivos con su estructura de carpetas
Principios de diseño: seguridad por capas, separación de responsabilidades, resilencia, mobile-first, dark mode, performance
Checklist de implementación con checkboxes para seguir el proceso
Todo está escrito para que lo puedas copiar a cualquier otro repo y seguirlo como una guía completa sin dependencia de Terraleads.

AHORA CREA UN PROMPT PARA QUE CURSOR LO EJECUTE EN OTRO PROYECTO. QUE CREE UN PLAN Y SE EJECUTE AUTOMATICAMENTE SIN AUTORIZACION

ADMIN_CONTROL_CENTER_PROMPT.md(new)
+432
# Admin Control Center — Prompt para Cursor Agent> Copia todo el contenido de abajo (desde la línea `---` hasta el final) y pégalo como mensaje en Cursor con **Agent mode** activado y **auto-run** habilitado (desactiva la confirmación manual). Cursor ejecutará todo el proceso automáticamente.---Eres un arquitecto senior especializado en React + TypeScript + Supabase. Tu misión es construir un **Admin Control Center** completo en este proyecto. Ejecuta TODAS las fases sin detenerte a preguntar. No pidas confirmación. Analiza el proyecto, adáptate a sus convenciones y construye todo.## INSTRUCCIONES GLOBALES1. **Antes de escribir código**, analiza el proyecto:   - Lee `package.json` para saber qué dependencias hay (UI library, routing, state management)   - Lee la estructura de `src/` para entender la organización de carpetas   - Busca el cliente de Supabase (normalmente en `src/lib/`, `src/integrations/` o `src/utils/`)   - Busca el AuthContext o equivalente para saber cómo se gestiona la autenticación   - Busca si ya existe un componente `ProtectedRoute` o similar   - Busca el router principal (normalmente `App.tsx`, `router.tsx` o similar)   - Identifica la librería de componentes UI base (shadcn/ui, MUI, Chakra, Ant, o custom)   - Busca si ya existe algo de admin para no duplicar2. **Adáptate a las convenciones del proyecto**: usa el mismo estilo de imports, naming, estructura de carpetas, librería UI, etc.3. **Instala dependencias** solo si faltan. Verifica antes de instalar:   - `recharts` — para gráficos   - `lucide-react` — para iconos (o usa la librería de iconos que ya use el proyecto)   - `sonner` — para toasts (o usa la que ya exista)4. **Ejecuta todo en orden**. No te saltes ninguna fase. Cada fase depende de la anterior.---## FASE 1: MIGRACIÓN SQL — Tablas baseCrea el archivo `supabase/migrations/[timestamp]_admin_panel.sql` con:```sql-- ============================================-- ADMIN PANEL — Tables & Core Functions-- ============================================-- Whitelist de admins (nadie puede auto-promoverse)CREATE TABLE IF NOT EXISTS admin_users (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,  role TEXT NOT NULL DEFAULT 'admin' CHECK (role IN ('admin', 'super_admin')),  granted_by UUID REFERENCES auth.users(id),  created_at TIMESTAMPTZ DEFAULT now(),  UNIQUE(user_id));ALTER TABLE admin_users ENABLE ROW LEVEL SECURITY;CREATE POLICY "No direct access to admin_users" ON admin_users FOR ALL USING (false);-- Función: verificar si un usuario es adminCREATE OR REPLACE FUNCTION is_admin(p_user_id UUID)RETURNS BOOLEANLANGUAGE plpgsql SECURITY DEFINER SET search_path = publicAS $$BEGIN  RETURN EXISTS (SELECT 1 FROM admin_users WHERE user_id = p_user_id);END;$$;-- Helper: registrar acción admin en audit logCREATE OR REPLACE FUNCTION _admin_log_action(  p_action TEXT, p_target_type TEXT DEFAULT NULL,  p_target_id TEXT DEFAULT NULL, p_metadata JSONB DEFAULT '{}'::jsonb)RETURNS VOIDLANGUAGE plpgsql SECURITY DEFINER SET search_path = publicAS $$BEGIN  INSERT INTO admin_audit_logs (admin_user_id, action, target_type, target_id, metadata)  VALUES (auth.uid(), p_action, p_target_type, p_target_id, p_metadata);END;$$;-- Audit logsCREATE TABLE IF NOT EXISTS admin_audit_logs (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  admin_user_id UUID NOT NULL REFERENCES auth.users(id),  action TEXT NOT NULL,  target_type TEXT,  target_id TEXT,  metadata JSONB DEFAULT '{}'::jsonb,  ip_address TEXT,  created_at TIMESTAMPTZ DEFAULT now());CREATE INDEX IF NOT EXISTS idx_audit_logs_admin ON admin_audit_logs(admin_user_id);CREATE INDEX IF NOT EXISTS idx_audit_logs_action ON admin_audit_logs(action);CREATE INDEX IF NOT EXISTS idx_audit_logs_created ON admin_audit_logs(created_at DESC);ALTER TABLE admin_audit_logs ENABLE ROW LEVEL SECURITY;CREATE POLICY "No direct access to admin_audit_logs" ON admin_audit_logs FOR ALL USING (false);-- Feature flagsCREATE TABLE IF NOT EXISTS feature_flags (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  key TEXT NOT NULL UNIQUE,  name TEXT NOT NULL,  description TEXT,  enabled BOOLEAN NOT NULL DEFAULT false,  rollout_percentage INTEGER NOT NULL DEFAULT 0 CHECK (rollout_percentage BETWEEN 0 AND 100),  target_users UUID[] DEFAULT '{}',  metadata JSONB DEFAULT '{}'::jsonb,  created_by UUID REFERENCES auth.users(id),  updated_by UUID REFERENCES auth.users(id),  created_at TIMESTAMPTZ DEFAULT now(),  updated_at TIMESTAMPTZ DEFAULT now());ALTER TABLE feature_flags ENABLE ROW LEVEL SECURITY;CREATE POLICY "No direct access to feature_flags" ON feature_flags FOR ALL USING (false);-- System config (key-value)CREATE TABLE IF NOT EXISTS system_config (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  key TEXT NOT NULL UNIQUE,  value JSONB NOT NULL DEFAULT '{}'::jsonb,  category TEXT NOT NULL DEFAULT 'general'    CHECK (category IN ('api_limits','email_limits','features','thresholds','general')),  description TEXT,  updated_by UUID REFERENCES auth.users(id),  updated_at TIMESTAMPTZ DEFAULT now());ALTER TABLE system_config ENABLE ROW LEVEL SECURITY;CREATE POLICY "No direct access to system_config" ON system_config FOR ALL USING (false);INSERT INTO system_config (key, value, category, description) VALUES  ('api_rate_limit_per_minute', '60'::jsonb, 'api_limits', 'Max API requests per minute per user'),  ('api_daily_limit', '10000'::jsonb, 'api_limits', 'Max API requests per day per user'),  ('session_timeout_minutes', '480'::jsonb, 'thresholds', 'Session timeout in minutes')ON CONFLICT (key) DO NOTHING;INSERT INTO feature_flags (key, name, description, enabled, rollout_percentage) VALUES  ('new_dashboard', 'Nuevo Dashboard', 'Interfaz rediseñada', false, 0),  ('beta_features', 'Beta Features', 'Funcionalidades experimentales', false, 0),  ('advanced_reports', 'Reportes Avanzados', 'Generador de reportes avanzado', false, 0)ON CONFLICT (key) DO NOTHING;```## FASE 2: MIGRACIÓN SQL — RPCsCrea el archivo `supabase/migrations/[timestamp+1]_admin_rpcs.sql`. **IMPORTANTE**: Antes de escribir las RPCs, analiza qué tablas existen en el proyecto (busca en otras migraciones o en `supabase/migrations/`). Adapta las subqueries a las tablas reales del proyecto. Si no existe una tabla referenciada, omite esa métrica o usa 0.Las RPCs que debes crear son:### RPCs de lectura:1. **`admin_get_role()`** — Devuelve el rol del admin actual (`admin` o `super_admin`)2. **`admin_get_overview()`** — Métricas generales: total_users (de auth.users), y conteos de las tablas principales del proyecto3. **`admin_get_dashboard_metrics()`** — KPIs extendidos: users activos hoy/semana/mes, MRR/ARR si hay suscripciones, conteos de recursos principales4. **`admin_get_users(p_page, p_per_page, p_search, p_plan_filter, p_sort_by, p_sort_dir)`** — Listado paginado de usuarios desde auth.users LEFT JOIN profiles LEFT JOIN subscriptions (adaptar a las tablas que existan)5. **`admin_get_user_detail(p_target_user_id)`** — Detalle completo de un usuario: perfil, suscripción, stats, actividad reciente6. **`admin_get_revenue_analytics(p_months)`** — Si hay tabla de suscripciones: MRR, ARR, by_plan, monthly_signups, conversion_rate, ARPU. Si no hay suscripciones, devolver estructura vacía7. **`admin_get_api_usage(p_days)`** — Si hay tabla de api_usage/api_logs: total_calls, by_type, daily_usage, top_users. Si no, devolver vacío8. **`admin_get_email_analytics(p_days)`** — Si hay tablas de email: campaigns, contacts, templates. Si no, devolver vacío9. **`admin_get_system_health()`** — pg_database_size, pg_stat_activity, conteos de tablas principales, errores recientes si hay tabla de logs10. **`admin_get_system_logs(p_page, p_per_page, p_operation_filter)`** — Si hay tabla system_logs, paginar. Si no, devolver vacío11. **`admin_get_audit_logs(p_page, p_per_page, p_action_filter, p_search)`** — Paginar admin_audit_logs con JOIN a profiles12. **`admin_get_feature_flags()`** — Lista de feature flags13. **`admin_get_config()`** — Lista de system_config### RPCs de mutación (todas con logging):14. **`admin_update_subscription(p_target_user_id, p_plan, p_quota_monthly)`** — Si hay tabla subscriptions15. **`admin_suspend_user(p_target_user_id, p_reason)`** — UPDATE auth.users SET banned_until16. **`admin_unsuspend_user(p_target_user_id)`** — UPDATE auth.users SET banned_until = NULL17. **`admin_delete_user(p_target_user_id)`** — Con protección anti-admin. DELETE cascada de datos del usuario18. **`admin_grant_admin(p_target_user_id, p_role)`** — Solo super_admin puede ejecutar19. **`admin_revoke_admin(p_target_user_id)`** — Solo super_admin, no puede revocarse a sí mismo20. **`admin_upsert_feature_flag(...)`** — UPSERT en feature_flags21. **`admin_delete_feature_flag(p_key)`** — DELETE en feature_flags22. **`admin_update_config(p_key, p_value)`** — UPDATE en system_config**PATRÓN para TODA RPC admin**:```sqlCREATE OR REPLACE FUNCTION admin_xxx(...)RETURNS JSONBLANGUAGE plpgsql SECURITY DEFINER SET search_path = publicAS $$BEGIN  IF NOT is_admin(auth.uid()) THEN    RAISE EXCEPTION 'Forbidden: admin access required';  END IF;  -- lógica  -- para mutaciones: PERFORM _admin_log_action('action', 'type', 'id'::text, metadata);  RETURN jsonb_build_object(...);END;$$;```## FASE 3: TIPOS TYPESCRIPTCrea `src/types/admin.ts` con interfaces que reflejen EXACTAMENTE lo que devuelven las RPCs:- `AdminOverview`- `AdminDashboardMetrics`- `AdminUser` y `AdminUsersResponse` (con paginación)- `AdminUserDetail` (profile, subscription, stats, recent_activity)- `AdminRevenueAnalytics` (mrr, arr, by_plan, monthly_signups, conversion_rate, arpu)- `AdminApiUsage` (total_calls, by_type, daily_usage, top_users)- `AdminAuditLog` y `AdminAuditLogsResponse`- `AdminEmailAnalytics`- `AdminSystemHealth` (database, users, api, storage, recent_errors)- `AdminSystemLog` y `AdminSystemLogsResponse`- `AdminFeatureFlag`- `AdminConfigEntry`- `AdminAction` (union type de todas las acciones posibles)## FASE 4: SERVICIOSCrea la carpeta `src/services/admin/` con un servicio por dominio:1. **`AdminDashboardService.ts`** — `isAdmin()`, `getAdminRole()`, `getOverview()`, `getDashboardMetrics()` con fallback2. **`AdminUserService.ts`** — `getUsers()`, `getUserDetail()`, `updateSubscription()`, `suspendUser()`, `unsuspendUser()`, `deleteUser()`, `grantAdmin()`, `revokeAdmin()`. Incluye fallback a query directo en profiles si la RPC falla3. **`AdminFinanceService.ts`** — `getRevenueAnalytics(months)`4. **`AdminApiUsageService.ts`** — `getApiUsage(days)`5. **`AdminLogsService.ts`** — `getSystemLogs(...)`, `getAuditLogs(...)`6. **`AdminEmailService.ts`** — `getEmailAnalytics(days)`7. **`AdminSystemService.ts`** — `getSystemHealth()`8. **`AdminFeatureFlagsService.ts`** — `getFeatureFlags()`, `upsertFeatureFlag(...)`, `deleteFeatureFlag(key)`9. **`AdminConfigService.ts`** — `getConfig()`, `updateConfig(key, value)`10. **`index.ts`** — Barrel export de todos los serviciosCrea `src/services/AdminService.ts` como facade:```typescriptexport const AdminService = {  isAdmin: AdminDashboardService.isAdmin,  getAdminRole: AdminDashboardService.getAdminRole,  // ... métodos más usados de cada servicio};```**PATRÓN de cada servicio**:```typescriptimport { supabase } from '@/integrations/supabase/client'; // o el path real del clienteimport { toast } from 'sonner'; // o la librería de toasts del proyectoimport type { TipoRetorno } from '@/types/admin';export const AdminXxxService = {  async metodo(): Promise<TipoRetorno | null> {    try {      const { data, error } = await supabase.rpc('admin_xxx');      if (error) { console.error('Error:', error.message); return null; }      return data as unknown as TipoRetorno;    } catch { return null; }  },};```Para mutaciones, añadir `toast.success()` en éxito y `toast.error()` en error.## FASE 5: COMPONENTES COMPARTIDOSCrea `src/components/admin/shared/` con estos componentes. USA LA LIBRERÍA UI QUE YA EXISTA EN EL PROYECTO (shadcn/ui Card, Button, etc.). Si el proyecto usa MUI, Chakra o Ant, adapta:1. **`AdminMetricCard.tsx`** — Card con: icon (top-right, bg-primary/10), label (text-sm muted), value (text-2xl bold), subtitle (text-xs muted), trend opcional (emerald si positivo, red si negativo)2. **`AdminChart.tsx`** — Wrapper de Recharts: props title, icon, data, type (area|bar|line), dataKey, xKey. Con gradients para area, border-radius para bars, tooltips estilizados3. **`AdminStatusBadge.tsx`** — Badge con variantes: active/healthy (emerald), suspended/error (red), warning (amber), draft/default (slate), sent (blue). Con dot opcional4. **`AdminConfirmDialog.tsx`** — AlertDialog con: título, descripción, variante destructive, campo de texto `requireConfirmation` que obliga a escribir una palabra para confirmar. Loading state en el botón5. **`AdminExportButton.tsx`** — DropdownMenu con opciones CSV y JSON## FASE 6: LAYOUTCrea `src/components/admin/AdminLayout.tsx`:- **Header sticky** con: icono Shield, título "Admin Control Center", badge de rol (admin/super_admin), botón "Volver al dashboard"- **Sidebar desktop** (hidden md:flex, w-56 lg:w-64): navegación agrupada en secciones:  - Principal: Overview  - Gestión: Usuarios, Suscripciones  - Analytics: Finanzas, Uso de API, Email  - Sistema: System Health, Logs, Feature Flags, Configuración- **Mobile nav** (md:hidden): botón que despliega dropdown con todos los items- **Active state**: bg-primary/10 text-primary para el item activo- **Main content**: flex-1, overflow-y-auto, padding responsive (p-4 sm:p-6 lg:p-8)Los iconos de cada sección (usa lucide-react o la librería de iconos del proyecto):- Overview → LayoutDashboard- Usuarios → Users- Suscripciones → CreditCard- Finanzas → DollarSign- Uso de API → Activity- Email → Mail- System Health → HeartPulse- Logs → ScrollText- Feature Flags → Flag- Configuración → Settings## FASE 7: GUARDCrea `src/components/admin/AdminGuard.tsx`:```1. Usa useAuth() (o el hook de auth del proyecto) para obtener user y loading2. useState isAdmin = null (null = loading, true/false = resolved)3. useEffect: si user existe → AdminService.isAdmin().then(setIsAdmin)4. Si loading o isAdmin === null → mostrar loading con Shield icon animado5. Si !user o !isAdmin → Navigate to /dashboard (o la ruta principal)6. Si todo OK → renderizar children```## FASE 8: PÁGINASCrea `src/pages/admin/` con 11 páginas. TODAS siguen el mismo patrón:```1. Estado: data, loading2. useEffect: cargar datos del servicio correspondiente3. Loading: AdminLayout + skeleton grid4. Error: AdminLayout + mensaje centrado5. Contenido: AdminLayout + título + subtítulo + grids de métricas + gráficos + tablas```### AdminOverview.tsx- Carga: getDashboardMetrics() + getRevenueAnalytics()- KPIs primarios (4 cards): Total usuarios, MRR, ARR, Usuarios activos semana- KPIs secundarios (4 cards): Nuevos hoy, API requests, Emails, Leads/recursos- Gráficos (2 cols): Signups por mes (area), Revenue por plan (bar)- Bottom (2 cols): Distribución de planes (lista), Métricas clave (conversion rate, ARPU, etc.)### AdminUsers.tsx- Carga: getUsers() con paginación- Controles: Input de búsqueda, Select de filtro por plan, indicador de resultados- Tabla responsive con columnas: Email, Nombre, Plan, Uso, Fecha registro, Acciones- Paginación: botones Anterior/Siguiente con indicador de página- Click en fila → navegar a /admin/users/:id### AdminUserDetail.tsx- Carga: getUserDetail(userId) con useParams()- Header: email, nombre, fecha, botón volver- Grid de stats (4 cards): leads, búsquedas, campañas, contactos- Sección suscripción: plan actual, quota, uso, botón cambiar plan- Sección API keys: tabla de keys activas- Sección actividad reciente: últimas búsquedas y uso de API- Acciones: Suspender (con AdminConfirmDialog), Eliminar (con requireConfirmation), Grant/Revoke admin (solo si super_admin)### AdminSubscriptions.tsx- Vista centrada en suscripciones de todos los usuarios- Tabla con columnas: email, plan, quota, uso, %, acciones### AdminFinance.tsx- Carga: getRevenueAnalytics()- KPIs: MRR, ARR, ARPU, Conversion Rate, Total paying- Gráficos: Monthly signups (area), Revenue by plan (bar)- Tabla: desglose por plan (plan, count, revenue)### AdminApiUsage.tsx- Carga: getApiUsage()- KPIs: total calls, total amount- Gráficos: daily usage (line), by type (bar)- Tabla: top users por consumo### AdminEmailAnalytics.tsx- Carga: getEmailAnalytics()- KPIs: campaigns total, sent, draft, contacts, lists, templates- Gráfico: daily activity (area)- Tabla: recent campaigns- Cards: campaigns by status### AdminSystemHealth.tsx- Carga: getSystemHealth()- Cards de estado con AdminStatusBadge: Database (size, connections), Users (total, active_24h), API (requests), Email (configs), Storage- Tabla: recent errors (si hay)### AdminLogs.tsx- Tabs: System Logs | Audit Logs- Cada tab: tabla paginada con filtros- System logs: operation, timestamp, details- Audit logs: admin_email, action, target_type, target_id, metadata, created_at### AdminFeatureFlags.tsx- Carga: getFeatureFlags()- Tabla/cards con: nombre, key, enabled toggle, rollout %, target users count- Botón "Nuevo flag" → formulario (dialog o inline)- Editar: dialog con campos key, name, description, enabled, rollout_percentage- Eliminar: AdminConfirmDialog con requireConfirmation del key### AdminConfig.tsx- Carga: getConfig()- Agrupado por categoría (api_limits, email_limits, features, thresholds, general)- Cada config: key, value editable, description, updated_at- Botón guardar por config individual## FASE 9: RUTASModifica el archivo de rutas del proyecto (busca `router.tsx`, `App.tsx` o equivalente):1. Añade lazy imports para las 11 páginas admin2. Añade las rutas envueltas en `<ProtectedRoute>` (o el guard de auth existente) + `<AdminGuard>`:```/admin                → AdminOverview/admin/users          → AdminUsers/admin/users/:userId  → AdminUserDetail/admin/subscriptions  → AdminSubscriptions/admin/finance        → AdminFinance/admin/api-usage      → AdminApiUsage/admin/email-analytics → AdminEmailAnalytics/admin/system         → AdminSystemHealth/admin/logs           → AdminLogs/admin/feature-flags  → AdminFeatureFlags/admin/config         → AdminConfig```## FASE 10: SEGURIDAD1. Si existe `robots.txt` en `public/`, añade `Disallow: /admin`2. Si no existe, créalo con contenido básico incluyendo el Disallow## FASE 11: VERIFICACIÓN FINAL1. Ejecuta `npx tsc --noEmit` o el typecheck del proyecto para verificar que no hay errores de tipos2. Si hay errores, corrígelos3. Ejecuta el linter si está configurado y corrige errores## NOTAS CRÍTICAS- **NO uses clases** para los servicios. Usa objetos con métodos async (patrón module)- **NO crees un contexto** para admin. Los servicios se llaman directamente desde las páginas- **ADAPTA los imports** al alias del proyecto (`@/`, `~/`, `src/`, etc.)- **ADAPTA el cliente Supabase** al path real del proyecto- **ADAPTA el hook de auth** al que ya exista (useAuth, useUser, useSession, etc.)- **ADAPTA los componentes UI** a la librería que ya use el proyecto- **Si una tabla referenciada en una RPC no existe**, devuelve 0 o array vacío en vez de fallar- **Cada RPC de mutación** DEBE llamar a `_admin_log_action()` antes de retornar- **Todas las RPCs** son `SECURITY DEFINER SET search_path = public` y empiezan verificando `is_admin(auth.uid())`- **El dark mode** debe funcionar. Usa variables CSS de Tailwind o la convención del proyecto- **Los skeletons de loading** deben tener la misma estructura que el contenido real para evitar layout shift- **Errores de las RPCs** deben mostrarse como toast, no como errores en consola silenciosos- **Los fallbacks** en servicios son opcionales pero recomendados para robustezEJECUTA TODO. NO PREGUNTES. NO TE DETENGAS. ANALIZA EL PROYECTO, ADÁPTATE Y CONSTRUYE.
Listo. El prompt está en:

docs/ADMIN_CONTROL_CENTER_PROMPT.md

Cómo usarlo en otro proyecto:
Abre el proyecto en Cursor
Ve a Settings > Features > Auto-Run y actívalo (para que no pida confirmación en cada paso)
Abre el chat en modo Agent (no Ask, no Plan)
Copia todo el contenido del archivo desde la línea --- hasta el final
Pégalo como mensaje y dale Enter
Cursor ejecutará las 11 fases secuencialmente sin detenerse
Lo que hace el prompt:
Fase	Acción
0	Analiza el proyecto (dependencias, estructura, convenciones, auth, UI library)
1	Crea la migración SQL con las 4 tablas + RLS + helpers
2	Crea las 22 RPCs adaptadas a las tablas reales del proyecto
3	Crea types/admin.ts con todas las interfaces
4	Crea 9 servicios + barrel export + facade
5	Crea 5 componentes compartidos (MetricCard, Chart, Badge, ConfirmDialog, ExportButton)
6	Crea el AdminLayout con sidebar + mobile nav
7	Crea el AdminGuard de autorización
8	Crea las 11 páginas completas
9	Modifica el router existente con lazy loading
10	Configura robots.txt
11	Verifica tipos y linter
La clave del prompt es que le dice explícitamente que analice primero el proyecto y se adapte a sus convenciones (alias de imports, cliente Supabase, librería UI, hook de auth, tablas existentes). No es un copy-paste rígido — es una receta que se adapta al contexto.